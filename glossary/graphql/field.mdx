---
title: field
---

A unit of data that belongs to a `type` in a schema. Every GraphQL query or mutation requests one or more fields.

```graphql
type Person {
  id: ID!
  name: String
  age: Int
}
```

[Learn more about fields.](https://graphql.com/learn/the-query/#querying-with-types-and-fields)
